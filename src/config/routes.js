const routes = {
  dashboard: '/',
  product: '/products',
  addProduct: '/product-add',
  updateProduct: '/product-update/:slug',
  discount: '/discount',
  order: '/order',
};

export default routes;
